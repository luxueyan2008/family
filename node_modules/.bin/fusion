#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../fusion/bin/fusion" "$@"
  ret=$?
else 
  node  "$basedir/../fusion/bin/fusion" "$@"
  ret=$?
fi
exit $ret
